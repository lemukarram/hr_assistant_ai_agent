#!/bin/bash
# This script is no longer used.
# The LLM is served by an external OpenAI-compatible API endpoint.
# See LLM_BASE_URL in .env or docker-compose.yml.
echo "Ollama is not used in this deployment. LLM is provided via LLM_BASE_URL."
exit 0
